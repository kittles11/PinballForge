# Cocos Creator 3.8.x + Cline 开发规则

## 1. 组件与属性编写规范
- 所有自定义组件必须使用 `@ccclass('ClassName')` 并 `export class ClassName extends Component`
- 严禁使用非空断言修饰 `@property`（如 `node: Node = null!`），所有 `@property` 应定义为可选类型或赋予合理的默认值
- 任何通过 `@property` 引入的节点或组件，在使用前必须判空（如 `if (!this.targetNode) { ... }`）

## 2. 节点查找与生命周期
- 避免直接在 `onLoad` 中访问其他单例或跨节点的未初始化属性；复杂初始化优先放到 `start()` 或显式的方法中
- 查找子节点时使用可选链与安全转换：`const comp = this.node.getChildByName('Foo')?.getComponent(Foo);`

## 3. 架构与解耦
- 模块间通信统一使用全局事件总线（EventBus），避免脚本间相互 import 导致循环依赖（Circular Dependency）
- 资源（Prefab/Audio/Texture）如需代码动态加载，请统一放置在 `assets/resources/` 目录下，并使用 `resources.load`
---

# Cocos Creator 3.x + 微信小游戏开发规范（cocos-wx-minigame-rules）

你在使用 Cocos Creator 3.x（TypeScript）开发微信小游戏。以下规则蒸馏自三本权威实战书籍（黄鸿信《Cocos Creator 3.x 游戏开发入门与实战》、李艺《微信小游戏开发》前端篇/后端篇），是硬性规范，与用户需求冲突时先提示冲突再遵循用户。

## 铁律（违反即产生真机 bug）

1. 模拟器通过 ≠ 真机通过：上线前必须在 iOS/Android 真机、PC 微信客户端实测。手机端不支持 Canvas 阴影、渐变填充、`#RRGGBBAA` 十六进制 RGBA 颜色、斜体字体——渐变/阴影效果改用预制图片，RGBA 改纯色。
2. 本地资源路径禁止 `./` 前缀：写 `static/images/a.png`。
3. 无 DOM：`document.getElementById`→`wx.createCanvas()`；`new Image()`→`wx.createImage()`；`new Audio()`→`wx.createInnerAudioContext()`；`addEventListener`→`wx.onTouchStart/Move/End/Cancel`（全局监听，坐标取 `res.touches[0].clientX/Y` 或 `res.changedTouches[0]`）。
4. 音频：`currentTime` 只读（重置用 `seek(0)`）；自然播完用 `onEnded` 而非 `onStop`；循环用 `loop=true` + `autoplay=true`；格式固定 mp3，不写 canPlayType 兼容。
5. 授权：`scope.userInfo` 必须用 `wx.createUserInfoButton`（禁止 wx.authorize 直弹）；播放声音、绘制普通 UI 不需要授权；`scope.userLocation` 可直接 authorize。
6. 事件监听 on/off 严格配对，回调必须命名函数（off 参数须与 on 完全一致）；组件销毁时解绑。
7. 定时器 id 必须保存并在结束/重启/销毁时 clear。
8. 异步回调（定时器/事件/rAF）内使用 this 必须 bind。
9. 所有异步调用 `.catch(console.log)`；Console 红错必修，灰警告可忽略。
10. Box2D 碰撞回调需勾选刚体 EnabledContactListener；Box2D 下移动父节点不带动子节点。
11. 服务端时间用 `db.serverDate()`，禁止客户端时间做记录。
12. 机密信息（key/secret）不放前端。
13. 重复启动 Tween 前先 stop 旧实例，并用状态变量防重入。

## 工程规范

- 目录：`scenes/ scripts/ sources/ prefabs/ resources/ animations/`（resources 只放动态加载资源）。
- 设计分辨率：横版 1280×720，竖版 720×1280；2D 元素必须挂 Canvas 下。
- 脚本：一个文件一个类，文件名=类名；组件类 `@ccclass` + `extends Component`；编辑器可见属性加 `@property`。
- 生命周期：onLoad(初始化)→onEnable→start(依赖初始状态)→update(每帧逻辑)→lateUpdate→onDisable→onDestroy(清理：off 事件、clear 定时器、stop 缓动)。
- 碰撞选型：少量对象用距离判断；需要物理效果才用物理系统（2D 仅查碰撞选 Builtin，需刚体/弹性选 Box2D；3D 选 PhysX）。
- 频繁创建销毁节点 → 对象池/循环复用。
- 跨端点击统一监听 TOUCH 事件（PC/移动端都触发），不用鼠标事件。
- 动画：简单位移缩放用 Tween；角色/复杂特效用 Spine/DragonBones；动画播完才切逻辑用 `Animation.EventType.FINISHED` 回调（回合切换等时序敏感操作必须放回调内）。

## 代码组织

- 游戏主循环六周期函数：init / start / run(纯计算) / render(纯渲染) / loop(rAF驱动) / end(清理结算)。
- 核心原则：控制数据而非控制渲染；计算与渲染严格分离。
- 管理者单例：`export default new XxxManager()`（Cocos 侧用节点常驻 + getInstance）；`initialized` 标记防重复初始化；方法返回统一 `{errMsg, data}`，`errMsg==='ok'` 为成功。
- 目录：`managers/`（data/audio/font/lbs/cloud/open_data/backend_api）+ `views/`（页面基类 Page、实体类）+ `libs/`（EventDispatcher 等）。
- 事件名定义为常量；模块解耦用自建 EventDispatcher(on/off/once/emit)。
- 页面切换：Game 持有 `#currentPage`，切换时旧页 end() 新页 start()。
- 参数化带默认值 `options?.x ?? 0`；内部状态 `#` 私有 + getter；重资源只加载一次。
- 异步转同步：promisify 包装 wx 异步接口 + async/await + `.catch(console.log)`；空值安全 `res?.data ?? {}`。

## 微信平台能力

- 后端选型阶梯（按需递进）：LocalStorage(10MB) → FileSystemManager(50MB, USER_DATA_PATH) → 云开发(个人首选：云函数免鉴权 getWXContext 取 openid；端上直操作云库只做非机密操作；分页 limit/skip/count；并发写用 `_.inc/_.push` 原子操作) → 自建服务器(koa2/iris+MySQL+frp 内网穿透)。
- UI 层级：自绘内容 < 原生按钮层（wx.createOpenSettingButton/GameClubButton/FeedbackButton/UserInfoButton，运行时托管、免清屏影响） < 系统弹窗（wx.showToast/showModal）。
- 系统事件：音频意外中断用 `wx.onAudioInterruptionBegin/End`（End 时检查 paused 再 play）；全局错误 `wx.onError` 记日志；防沉迷必接 `wx.checkIsUserAdvisedToRest`（确认后 exitMiniProgram）；振动 `vibrateShort` 失败降级 `vibrateLong` 双 catch。
- 好友排行榜：game.json 配 `openDataContext`；主域只能 postMessage 单向通信，好友数据不可传回主域；开放数据域不能 import 主域模块；KVData 格式 `{key:'rank', value:JSON.stringify({wxgame:{score,update_time}})}`，读写 key 一致。
- 广告：Banner `wx.createBannerAd`（创建即 show，监听 onError）；激励视频 `wx.createRewardedVideoAd`（手动 load，onClose 判 `isEnded` 才发奖励）。
- 字体：`wx.loadFont`；只需有限字符用 iconfont webfont 生成小字体文件。
- 背景图适配：取目标设备最小高宽比做最大图（≤2048px），居中裁切绘制，盖不住处纯色补底。
- 大资源（BGM/大图）放网络 URL，控制主包体积。

## 流程与产品

- 原型优先，"完成比完美重要"：核心玩法→能玩→重构打磨；原型允许脏代码与色块美术。
- 反馈由近到远：朋友（观察不干涉）→ 社群。
- 构建发布：至少一个场景；按需勾选参与构建场景；开 MD5 Cache；产物 `wechatgame/` 用微信开发者工具打开；AppID 用后台获取值。
- 上线材料：软著、icon、3~5 截图、80 字简介、10s 视频；变现无版号走广告（穿山甲/优量汇）。
- 调试：Chrome F12（Cocos 预览）；调试区 Storage/Network-Cloud；真机远程调试；构建报错看日志检索 error/failure。
- 排查顺序：先怀疑基础库版本 → 再怀疑平台差异（换端）→ 最后自己的代码。

## 禁止

document/window/DOM API；`./` 资源路径；依赖阴影/渐变/RGBA 色的关键 UI；匿名事件回调；不清理的定时器；每帧 new 对象；前端放密钥；客户端时间戳入服务端库；单机游戏引入服务器；主包塞大资源；一个文件多类；run 里混渲染逻辑。